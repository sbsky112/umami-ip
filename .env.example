# Database Configuration
DATABASE_URL=mysql://umami:umami@localhost:3306/umami
# or for PostgreSQL:
# DATABASE_URL=postgresql://umami:umami@localhost:5432/umami

# Database Type (postgresql or mysql)
DATABASE_TYPE=mysql

# Application Secret (REQUIRED for JWT tokens)
# Generate a random secret with: openssl rand -base64 32
APP_SECRET=your-random-secret-string-here

# Optional: Redis URL for caching
# REDIS_URL=redis://localhost:6379

# Optional: Disable IP collection
# DISABLE_IP_COLLECTION=false

# Optional: Custom IP header (if behind proxy)
# CLIENT_IP_HEADER=x-real-ip

# Optional: Ignore specific IPs
# IGNORE_IP=192.168.1.1,10.0.0.1

# Development Settings
NODE_ENV=development
# For production:
# NODE_ENV=production

# Optional: Enable query logging for debugging
# LOG_QUERY=true

# Optional: Database replica URL for read scaling
# DATABASE_REPLICA_URL=

# Cloud Mode (for Umami Cloud)
# CLOUD_MODE=false

# Cloudflare Turnstile Configuration (Optional)
# If not configured, Turnstile verification will be skipped
# NEXT_PUBLIC_TURNSTILE_SITE_KEY=your-site-key-here
# TURNSTILE_SECRET_KEY=your-secret-key-here